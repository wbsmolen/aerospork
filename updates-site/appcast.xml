<?xml version="1.0" encoding="utf-8" standalone="yes"?>
<rss xmlns:sparkle="http://www.andymatuschak.org/xml-namespaces/sparkle" version="2.0">
    <channel>
        <title>AeroSpork</title>
        <link>https://aerospork.app/appcast.xml</link>
        <description>Updates for AeroSpork, an i3-like tiling window manager for macOS.</description>
        <language>en</language>
        <item>
            <title>1.2.0</title>
            <pubDate>Sun, 13 Sep 2026 17:32:28 -0500</pubDate>
            <link>https://github.com/wbsmolen/aerospork</link>
            <sparkle:version>1.2.0</sparkle:version>
            <sparkle:shortVersionString>1.2.0</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>13.0</sparkle:minimumSystemVersion>
            <description><![CDATA[<p>Fixes focus moving on its own when an app is slow to respond, and a set of problems that a config migrated from AeroSpace ran into: empty workspaces disappearing, window rules waiting for a click, and a settings window that could open out of sight. Reported in <a href="https://github.com/wbsmolen/aerospork/issues/39">#39</a> and <a href="https://github.com/wbsmolen/aerospork/issues/40">#40</a>.</p>
<h2>Upgrade notes</h2>
<ul>
<li><strong>Declared workspaces always exist.</strong> Every workspace named in <code>workspaces</code> (with or without <code>mod</code>), in <code>persistent-workspaces</code>, or pinned to a monitor now exists even while empty. The default config declares <code>1-9</code>, so all nine appear in <code>list-workspaces --all</code>, the menu bar and <code>workspace next</code>/<code>prev</code> from launch. A workspace you reach only through a binding of your own is still created when you switch to it and released once empty.</li>
<li><strong>One copy at a time.</strong> Starting AeroSpork while it is already running prints &quot;AeroSpork is already running&quot; and exits. Debug and release builds can still run side by side.</li>
<li><strong>AeroSpork no longer manages its own windows.</strong> The settings window is never tiled or moved to a workspace.</li>
<li><strong>Four AeroSpace keys are ignored instead of fatal.</strong> <code>config-version</code>, <code>auto-reload-config</code>, <code>on-mode-changed</code> and <code>focus-follows-mouse</code> are reported and ignored. Previously any of them made AeroSpork reject the whole config and run its default.</li>
</ul>
<h2>Focus and unresponsive apps</h2>
<ul>
<li>A window whose app is slow to answer Accessibility requests is no longer treated as closed. Previously it was dropped, focus moved to another window, and the window could come back on a different workspace.</li>
<li>Focus moves after a window closes only if that window was focused. If macOS moves focus to another workspace because of the close, AeroSpork brings it back.</li>
<li>A late answer from a busy app no longer pulls you back across a workspace switch.</li>
<li>A minimized window comes back on its own workspace rather than the one you are on.</li>
<li>While an app is hung, a workspace switch waits about a second for it, not a second per window.</li>
<li><code>killall AeroSpork</code> exits within 10 seconds even when AeroSpork itself is stuck.</li>
</ul>
<h2>Window rules</h2>
<ul>
<li>A rule applies without clicking the window when the app was still starting, or when the window first looked like a popup.</li>
<li>A rule&#x27;s command list always runs to the end. Previously a refresh at the wrong moment could stop it after <code>layout tiling</code>.</li>
</ul>
<h2>Callbacks</h2>
<ul>
<li><code>exec-and-forget</code> run from <code>on-focused-workspace-changed</code> now receives <code>AEROSPORK_FOCUSED_WORKSPACE</code> and <code>AEROSPORK_PREV_WORKSPACE</code>, so the documented replacement for <code>exec-on-workspace-change</code> works.</li>
</ul>
<h2>Settings</h2>
<ul>
<li>Settings comes to the front when opened from the menu, with <code>aerospork open-settings</code>, or by opening AeroSpork again from Finder or Spotlight.</li>
<li>A change made just before closing the window is saved instead of dropped.</li>
<li>A saved change takes effect on screen immediately.</li>
<li>The General pane says which settings apply only to new workspaces, and that turning off a normalization does not undo earlier changes.</li>
</ul>
<h2>Coming from AeroSpace</h2>
<ul>
<li><code>persistent-workspaces</code> is supported.</li>
<li>When only an AeroSpace config exists, AeroSpork says that it is running its default config.</li>
<li>A string <code>if</code> in <code>[[on-window-detected]]</code> is rejected with an error that names the table form.</li>
<li>A command that still uses <code>AEROSPACE_*</code> variables or the <code>aerospace</code> CLI loads with a warning naming the line.</li>
<li>The guide has a new &quot;Coming from AeroSpace&quot; section.</li>
</ul>
<h2>Troubleshooting</h2>
<ul>
<li>AeroSpork logs a startup record with its version and whether verbose tracing is on, and logs focus changes it makes on its own under the <code>session</code> category.</li>
<li>The troubleshooting guide and bug report template give the right log subsystem for release builds and explain how to capture a verbose trace.</li>
</ul>
<p>Universal (arm64 + x86_64), signed with a Developer ID, notarized and stapled.</p>
<h2>Install</h2>
<pre>
brew install --cask wbsmolen/tap/aerospork
</pre>
<p>Or download <code>aerospork-v1.2.0.zip</code>, move <code>aerospork-v1.2.0/AeroSpork.app</code> to <code>/Applications</code>, and grant Accessibility permission when prompted. For the CLI, symlink the installed copy: <code>ln -s /Applications/AeroSpork.app/Contents/MacOS/aerospork-cli /usr/local/bin/aerospork</code>.</p>
<p><code>AeroSpork-1.2.0.zip</code> is the Sparkle update archive — the same app without the CLI, man pages or licence files. It exists for the updater; prefer the versioned zip for a manual install.</p>
]]></description>
            <enclosure url="https://github.com/wbsmolen/aerospork/releases/download/v1.2.0/AeroSpork-1.2.0.zip" length="6788074" type="application/octet-stream" sparkle:edSignature="UQc2nYG5f0JqX1NmBXbas3K+d2t6bI06pldKCLQ+wJv9vwwbIWet6/VuBw5klpAtWEHfEirbXkChRg6wo14MBQ=="/>
        </item>
        <item>
            <title>1.1.8</title>
            <pubDate>Sun, 02 Aug 2026 11:27:08 -0500</pubDate>
            <link>https://github.com/wbsmolen/aerospork</link>
            <sparkle:version>1.1.8</sparkle:version>
            <sparkle:shortVersionString>1.1.8</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>13.0</sparkle:minimumSystemVersion>
            <description><![CDATA[<h2>Settings, redesigned</h2>
<p>Every pane got a full pass — same seven panes, now behaving like a native macOS preferences window.</p>
<ul>
<li><strong>Native pane toolbar.</strong> The settings window uses the standard macOS preferences toolbar, remembers which pane you were on, and titles the window after it.</li>
<li><strong>Raw TOML is now a real editor.</strong> Line numbers, native Find (⌘F), horizontal scrolling instead of wrapping, restrained TOML highlighting, a section jump menu, cursor line/column readout, and parser errors that link to the failing line.</li>
<li><strong>Keys.</strong> Bindings grouped by what they do, search across every mode at once, one-click Duplicate, and a mode picker that stays usable however many modes you define.</li>
<li><strong>Monitors.</strong> The arrangement schematic is now the control: click a monitor to see its details and the workspaces pinned to it, then pin, move, or unpin workspaces from a menu right there. Assignments too rich for the editor are shown as such and preserved untouched — editing one workspace can no longer re-spell another&#x27;s regex or fallback list.</li>
<li><strong>The settings window is finally resizable.</strong> It was silently fixed at its ideal size; the documented 780×520 minimum is now real.</li>
<li><strong>Window Rules.</strong> Guided controls for the common cases — float/tile and send-to-workspace — with your exact command preserved verbatim whenever it says something the controls can&#x27;t.</li>
<li><strong>Gaps.</strong> A live preview, and &quot;same value&quot; links so symmetric gaps take one edit instead of four.</li>
<li><strong>Events.</strong> Real command placeholders, per-event explanations, and a heads-up when callbacks inherit your shell environment.</li>
</ul>
<h2>Also in this release</h2>
<ul>
<li>The standard &quot;About AeroSpork&quot; menu item is back.</li>
<li>Short-value fields (workspace names, the assignment picker) are sized to their content instead of stretching across the row.</li>
<li>Badge text contrast now meets WCAG AA in light mode, and the monitor schematic stays legible with asymmetric arrangements (an ultrawide above two 4Ks no longer overlaps its labels).</li>
<li>&quot;Choose app…&quot; in Window Rules now opens as a sheet on the settings window. It used to open behind everything — a menu-bar app can&#x27;t bring a detached panel forward — which read as the button hanging the app.</li>
<li>Backup timestamps are now locale-independent; if your system uses a non-Gregorian calendar, backups made by older versions may show raw filenames in the Restore menu once.</li>
</ul>
]]></description>
            <enclosure url="https://github.com/wbsmolen/aerospork/releases/download/v1.1.8/AeroSpork-1.1.8.zip" length="6721062" type="application/octet-stream" sparkle:edSignature="3bjx8nGP1oYcfavdhou6yUEWNjohtBs6WzyCJ0doNOQQQMTuyUPKk9zE9ZeLUE6CM0MUt74dPxW7NkAgRXLcCA=="/>
        </item>
        <item>
            <title>1.1.7</title>
            <pubDate>Thu, 30 Jul 2026 19:20:19 -0500</pubDate>
            <link>https://github.com/wbsmolen/aerospork</link>
            <sparkle:version>1.1.7</sparkle:version>
            <sparkle:shortVersionString>1.1.7</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>13.0</sparkle:minimumSystemVersion>
            <description><![CDATA[<p>Fixes the settings window slowly growing your config file.</p>
<h2>Saving from Settings no longer adds blank lines</h2>
<p>Every save through the settings window added a blank line before the section it rewrote, and dropped the newline at the end of the file. One save was invisible. But settings apply on a short delay, so a minute of adjusting things is several saves — and the file grew a line each time, leaving a widening gap in the middle and <code>\ No newline at end of file</code> at the bottom of every diff.</p>
<p>If your config lives in a dotfiles repository, this is the change that stops it showing up as noise there.</p>
<p>Nothing you wrote is touched. Blank lines you put in yourself stay exactly as they are — the fix only suppresses the ones the editor was adding.</p>
<p>Universal (arm64 + x86_64), signed with a Developer ID, notarized and stapled.</p>
<h2>Install</h2>
<pre>
brew install --cask wbsmolen/tap/aerospork
</pre>
<p>Or download <code>aerospork-v1.1.7.zip</code>, move <code>aerospork-v1.1.7/AeroSpork.app</code> to <code>/Applications</code>, and grant Accessibility permission when prompted. Optionally put <code>aerospork-v1.1.7/bin/aerospork</code> on your <code>PATH</code> for the CLI.</p>
<p><code>AeroSpork-1.1.7.zip</code> is the Sparkle update archive — the same app without the CLI, man pages or licence files. It exists for the updater; prefer the versioned zip for a manual install.</p>
]]></description>
            <enclosure url="https://github.com/wbsmolen/aerospork/releases/download/v1.1.7/AeroSpork-1.1.7.zip" length="6311356" type="application/octet-stream" sparkle:edSignature="j8DnGsc71nFqOKD/w4xEjLwB5CDmJCVt9wwgJV8OqNtPeL+VK2SzQ9vVxK0FeBd6fnnUoldsuWZt2jw329kxDg=="/>
        </item>
    </channel>
</rss>