/.idea
/.debug
/.release
/.site
/.man
# IDK, AppCode randomly creates this EMPTY file. I have no idea what this is
/default.profraw
.DS_Store
/.xcode-build
# Swift package manager
/.build
/.swiftpm
/.vscode
# External dependencies
/.deps
# Xcode build products (may contain personal paths)
/build

# For whatever local files that developers might want to keep there (I personally keep a separate `generated-html` git worktree here)
/.local

# XCode User settings
xcuserdata/
xcshareddata/
# Generated Xcode project (regenerated from project.yml)
/aerospork.xcodeproj
# Runtime/build junk
/dist
*.log
/nohup.out
/test_output.txt
# Tool dirs
/.serena

# Only the project's own design system is tracked under .claude/; everything else there is
# local tooling and does not belong in a public repo.
/.claude/*
!/.claude/skills/
/.claude/skills/*
!/.claude/skills/aerospork-design/
/.aerospork-Info.plist

# Browser-automation session dumps (page snapshots, console logs). Tooling output, not source.
/.playwright-mcp
